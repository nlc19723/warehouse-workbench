# StockHub 云端安全说明 · 🟢 v228.05

> 本文档说明当前云端同步的安全模型、已做的加固，以及**仍未闭环的真实风险**与下一步路线。
> 请仓库管理员/部署者在 Supabase 控制台按 `docs/storage-policies.md` 落实加固。

## 1. 当前架构（务必先读）

| 项 | 现状 |
|---|---|
| 云端形态 | **Supabase Storage 桶**（默认 `workbench-data`），**不是** Postgres 表 |
| 数据文件 | `data.json`（工作数据）、`base.json`（基准底账）、`settings.json`（偏好）、`stocktake.json`（盘点） |
| 访问凭证 | **`anon` key**（公开角色 key），**无 Supabase Auth 登录** |
| 鉴权隔离 | **无**——任何持有 anon key 的人，对该桶 4 个文件拥有完整读写权 |

> 应用自身的「库管员/管理员登录」是**本地登录**（存在本地 Dexie），与 Supabase 账户体系无关。
> 也就是说：云端侧**无法区分**是哪个库管员在操作，所有设备共享同一份云端数据。

## 2. 已做的两层加固

### 2.1 凭证防扫描混淆（v228.04）
- Supabase url/anonKey 由硬编码明文改为「4 段分割 + XOR + Base64」可逆轻量编码。
- 源码与「源码下载」产物中不再出现完整 JWT 头部特征串（eyJ 开头）与明文项目地址。
- **作用**：仅绕过平台风控的「关键词/特征串静态扫描」与源码泄露，让自动连接与弹窗预填不受影响。
- **不是加密**：XOR key 和密文都在前端，懂行的人开 DevTools 即可还原。详见 `tools/credential-tool.mjs`。

### 2.2 Storage 对象策略（v228.05，见 `docs/storage-policies.md`）
- 将桶设为**私有**（关闭公开 URL 直接拖库）。
- 给 `anon` 角色加 Storage 对象策略，**按精确文件名白名单**授权读写删，禁止触碰其它桶/路径。
- **作用**：消除「桶公开→免 key 读取」的泄露面；缩小 anon key 的作用范围（纵深防御）。

## 3. ⚠️ 残留风险（必须正视）

1. **anon key 仍等于全权**：上了 Storage 策略后，拿到 anon key 的人依然能读写这 4 个文件。
   混淆 ≠ 保密，策略 ≠ 鉴权。这是 Storage 架构的固有局限。
2. **无用户隔离**：云端无法区分库管员，任一设备可覆盖其他设备数据（应用层靠「读-合并-写」乐观锁尽力缓解，非强一致）。
3. **公开 URL 兜底**：`js/sync.js` 的 `getSettings()` 保留了一条带 `apikey` 头的公开 URL 兜底路径，
   桶设为私有后仍走鉴权（不会明文泄露），但属历史兼容代码，建议后续移除。

## 4. 下一步：真正的隔离（需另立项，非小修）

若要彻底消除「anon key 即全权」风险，必须引入**身份**与**服务端**：

| 方案 | 要点 | 代价 |
|---|---|---|
| **A. Supabase Auth + 每设备账号** | 每个库管员/设备一个 Supabase 用户，策略按 `auth.uid()` 隔离文件到各自前缀 | 改动登录与同步流程，需培训账号下发 |
| **B. 后端代理 + 真加密（AES-GCM）** | 前端不再持有 anon key，改为请求自建后端；密钥仅存服务端，云端存密文 | 引入后端服务（Node/Python 单端口），会改变纯静态部署形态，自动连接链路需重设计 |

> 方案 A/B 均**需另立项评审**，不在本静态 PWA 的小步迭代范围内。当前阶段先把 §2 两层加固落地，已能挡住最常见的两类暴露：源码泄露 key、桶公开拖库。

## 5. 立即行动清单（管理员）

- [ ] 在 Supabase 控制台执行 `docs/storage-policies.md`（全选执行）
- [ ] 确认桶 `workbench-data` 的「Public」开关已关闭
- [ ] 确认 4 条 `anon_*_workbench_data` 策略存在（`pg_policies` 可查）
- [ ] 永远不要把 `service_role` key 放进前端（当前用的是 `anon`，正确）
- [ ] 如有多仓/多组织需求，走方案 A 或 B 立项
