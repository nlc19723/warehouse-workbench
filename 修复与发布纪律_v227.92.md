# 仓库工作台 · 修复与优化全量归纳（v217 → v227.96）＋ 发布纪律

> 范围：自**第一次对话（v217）**起、一直到 **v227.96** 的全部修复与优化。
> 来源：逐份通读历史方案 / 审查报告 / 修复报告 / 源码版本注释（`css/style.css`、`js/*.js` 内 `🟢 v2xx.x` 标注）归纳，非抽样。
> 用途：① 给后来者一份"这仓库历史上都改过什么、为什么改"的地图；② 提炼反复栽的坑，形成可执行的修复遵守规则。

---

## 0. 一句话

> **这个仓库的修复史，本质只有三条主线反复交织**：① 让盘点业务跑对（三层架构 / 分派 / 补盘 / 汇总 / 序号）；② 让界面统一好看（暗黑模式 / 按钮 / 字号 / 凹陷感 / 移动端）；③ 让应用跑得稳（数据完整性 / 精度 / 同步 / 离线 / SW 缓存）。
> **而"发布翻车"几乎全部源自同一类低级错误：版本戳漏 bump + 改动后没立刻实测**。

---

## 一、版本时间线（里程碑速查）

| 版本 | 主题 | 关键动作 |
|---|---|---|
| **v217** | 盘点三层架构 + 账号权限 + 连接状态 | 执行/批次汇总/明细三层拆分；库管员 18 模块权限；任务分派（固化编码清单）；pull 合并；修 BUG-A 重复保存 / BUG-B 浮点精度 / 云端 task 覆盖 / createdAt 时序 / sheetId 格式 |
| v227.16 / 17 | 季度盘点 · 补盘入口 + 同步 | 开下一轮墓碑清任务；结束返回过期数据（未 await）；轮询被 hover 挡；双补盘入口收敛；离线云端待推队列 `wb_stocktake_cloud_q` |
| v227.24–28 | 盘点模块美化 | 卡片去厚阴影、白底翻号键、字段横排、输入框凹陷立体阴影 |
| v227.32 | 全局按钮基线 | 兜底裸 `<button>` 立体毛玻璃；状态色提饱和（解决莫兰迪低对比） |
| v227.33 | 移动端底部 Tab | 一级入口仅「查询 / 盘点」 |
| v227.34 | 筛选栏统一（雏形） | 搜索框独占一行、日期并排固定宽 |
| v227.35 | 移动端收起态 | 固定左侧图标栏常驻 |
| v227.43 | 收起态精简 | 不再显示模块名 tooltip；移除账号状态条 |
| v227.45 | 弹窗/胶囊/悬浮 | 胶囊居中、白底立体悬浮圆、弹窗自适应移动端 |
| v227.48 | iOS 防缩放 | `input:not(.as-input){font-size:16px!important}`（埋下后续字号冲突伏笔） |
| v227.49 | 弹窗宽度一致 | 退出弹窗单按钮不再缩到内容宽 |
| **v227.50** | 全局代码审查① | 版本一致性 PASS；5 类中风险：重导清空非事务 / 金额浮点 / await 无 catch / 全量加载 / favicon |
| **v227.52** | 数据库层 P0 | 9 个 getter 改走 `getRows()` 内存缓存 + `slice` 分页，消除翻页"两次全表扫描" |
| **v227.53** | 暗黑模式适配 | 侧栏/头部/按钮/盘点横幅/打印小票统一深色；CSS 替代 JS 内联硬编码 |
| v227.61 / 62 | 筛选栏分轨 | 移动端 `filter-bar-m` 纵向分轨；PC 筛选控件视觉统一 |
| v227.63 / 64 / 65 | 同步弹窗 / 列折叠 | 云端同步弹窗 2 排；`mobile-col-toggle` 尺寸回退 |
| v227.67 | 分派弹窗圈红说明 | （v227.69 随样式重构被移除） |
| **v227.69** | 全局代码审查② + 修复 | 解决 clearAll 事务化 / breach 整数分 / favicon；**闭环 AUDIT-008：`.as-input` 被 `@media` 漏闭合 `}` 吞噬 4 版本未生效** |
| **v227.70** | 审计修复 | AUDIT-004 写/同步路径有界重试 + Toast 兜底；AUDIT-005 游标/limit；AUDIT-003 金额浮点收口 10 处；AUDIT-007 删死 CSS |
| **v227.71** | UIUX 微体验 | 同步状态条三态脉冲；空状态教学化；扫描/计数微动效；版本更新提示打磨（滑入 + 反馈） |
| **v227.72** | 离线 / SW 回退缺陷 | SW 把 JS/CSS 回退成 HTML → 按导航/资源分治 + 核心 JS 预缓存 + 成功后 `cache.put` |
| **v227.73** | UI 统一改造 | 翻页键统一 `.wb-pager-btn`；字号 14→12.5px；凹陷感三层 inset 阴影统一；暗色模式 |
| **v227.74** | 移动端筛选栏统一排版 | `@media(max-width:768px)` 统一 11 模块；修 `flex-basis:100%` 覆盖 height 陷阱；`FilterLayout.balanceAll` 补 5 模块 |
| **v227.75** | 入库/订单模块补漏 | `.filter-row` 结构未走 v227.74 规则 → 扩展通用规则 + `orders.js` 第一行 DOM 重构 |
| v227.77 / 85 | CSS `@media` 闭合误放 | v227.77 把闭合 `}` 误放 3585 行 → 3419~3584 桌面规则被关进移动端块；v227.85 修复 |
| v227.82 / 83 | 字号/凹陷感再统一 | 全部下拉/输入/日期框对齐 `.filter-bar`/`.ss-trigger` 模板 |
| v227.84–87 | 盘点卡片紧凑化 | 按钮贴标题左、盘点号组并排、qr-actions 提层 |
| **v227.89–92** | 本轮：SW 白屏 + 缺文件 + 回归门禁 + 版本提示 + 4 项 UI/逻辑修复 | 见第六节 |
| v227.93–96 | 四轮：PC 订单/日期/盘点修改/操作列 UI 修复 → 云端恢复 ConstraintError 修复 → 云端恢复入口合并（最新+上一份）→ 导入弹窗三按钮并排文案 | 见主题 J / 第六节 |

---

## 二、按主题归纳的修复与优化

### 主题 A · 盘点业务核心（最复杂、最易出数据 bug）
| 版本 | 问题 | 修复 |
|---|---|---|
| v217 | 只有两层、无批次级视图；分派后结束会覆盖固化编码（`Object.assign` 缺失）；结束命名取到"结束那天" | 三层架构 + 任务合并写 + `createdAt` 分派时写、结束禁覆盖 + 批次命名 `batchName` |
| v217 | 保存即重复写行（BUG-A）；差异量浮点尾巴（BUG-B `-10.29000000000002`） | 按本人剔除已盘；`round2()` 统一精度 |
| v227.16 | 开下一轮只解闸，上一轮任务复活成"进入盘点"、概览残留 | 墓碑删除上一轮任务 + 清空本批次概览 |
| v227.16 | 点结束返回瞬间显示"已盘 0/5·未结束"（渲染抢跑） | `_publishQuarterOverview` 改 `await` 后再渲染 |
| v227.16 | 8s 轮询被 `:hover` 守卫挡死 → 鼠标停区域永远不刷新 | 改"最近 1.2s 内有交互"才跳过重渲 |
| v227.16 | 补盘场景"继续盘点"死按钮、放弃盘点误进表、双补盘入口、全盘完仍给补盘死入口 | 找 closed 任务走补盘；`_skipResumePrompt`；补盘收敛唯一出口；剩余未盘=0 隐藏 |
| v227.17 | 跨设备同步无离线重试队列，断网期间其他设备看不到最新状态 | 本地待推队列 `wb_stocktake_cloud_q` + `online` 事件补推 |
| v227.91 | 盘点号生成用 `max(已用)+1`，删除记录后序号不回填（与 v227.24 注释矛盾） | 只 bump 非 voided 记录，`used` 用 Set 取最小未占用序号 |

> **通性**：盘点业务的状态机（open→closed→补盘→下一轮）极易出现"上一版状态没清 / 渲染读到旧值 / 资源没释放"，每次改状态流转都要想"旧状态的残留如何回收"。

### 主题 B · 账号权限与连接状态
| 版本 | 问题 | 修复 |
|---|---|---|
| v217 | 断网仍显示"已连接" | `_connect()` 补 `verifyConnection()`；加 `offline`/`online` 监听 |
| v227.72 | 离线登录逻辑对，但 SW 把 CSS/JS 回退成 HTML → 白屏/登录点不动 | 导航请求才回退 index.html；资源请求未命中返回 504；核心 JS 预缓存 + 成功后写缓存 |

### 主题 C · 数据完整性 / 精度 / 同步（审计驱动）
| 版本 | 问题 | 修复 |
|---|---|---|
| v227.50 / 70 | 重导清空非事务 → 半清库 | `db.transaction('rw',[11表],...)` 包裹，失败回滚 |
| v227.50 / 70 | 金额浮点漂移（`0.1+0.2` 类） | `TableUtils.sumMoney` 整数分聚合（先 `Math.round(值*100)` 再累加） |
| v227.50 / 70 | 写/同步路径 `await` 无 catch → 静默失败 | `_uploadWithRetry`（有界退避）+ `Toast.warn/error` 显式提示 |
| v227.50 / 52 | 翻页每次 `toArray()` 全量 + 两次全扫描 | 9 个 getter 改 `getRows()` 缓存 + `slice` 分页（实测 175ms→0.1ms） |
| v227.69 | 休眠索引既不被读用、删也不提速（实测 A/B） | P1 决定不做，缓存已消除真正卡顿 |

### 主题 D · UI 统一（按钮 / 翻页 / 字号 / 凹陷感）
| 版本 | 问题 | 修复 |
|---|---|---|
| v227.3 / 32 | 三套并行玻璃按钮 + 内联补偿；裸 `<button>` 无样式 | 盘点内统一立体毛玻璃；全局按钮基线兜底 |
| v227.53 | 状态色莫兰迪低对比，小字号/移动端看不清 | 提饱和度，与深色主题对齐 |
| v227.73 | 翻页键各模块长得不一样（字符 ◀▶ / 灰边框） | 抽出 `.wb-pager-btn`（CSS border 黑三角，跨浏览器一致） |
| v227.73 | 日期/下拉框比其余大一号（被 v227.48 防缩放提到 16px） | 统一 12.5px + `!important` 覆盖；凹陷感三层 inset 阴影模板 |
| 按钮审计 | 危险操作是 `.secondary`+内联红，无语义类 | 规范 `.btn--danger`；最高杠杆点 `modal.js:104` 一行切换 |

### 主题 E · 暗黑模式
| 版本 | 问题 | 修复 |
|---|---|---|
| v227.53 | 缺深色适配；打印小票/提示横幅靠 JS 内联硬编码 | 侧栏/头部/按钮/5 类提示横幅/打印小票统一深色；CSS 替代内联 |

### 主题 F · 移动端适配（最高频返工区）
| 版本 | 问题 | 修复 |
|---|---|---|
| v227.33 | 移动端无一级入口 | 底部 Tab 栏（查询/盘点） |
| v227.35 | 收起态主内容被挤 | 固定左侧图标栏常驻 |
| v227.48 | iOS 聚焦自动放大 | `input{font-size:16px!important}`（代价：后续 12.5px 控件需 `!important` 反覆盖） |
| v227.45 / 49 | 弹窗在移动端不自适应 / 宽度不一致 | 弹窗自适应撑满；退出弹窗统一宽 |
| v227.74 / 75 | 筛选栏右边缘不对齐、高度不一致、561–768 错乱、侧栏溢出 | `@media(max-width:768px)` 统一 12 模块；修 `flex-basis:100%` 覆盖 height；补 `balanceAll` |
| v227.91 | 移动端折叠列按钮挂在 `#contentArea` 而非盘点进度条 | `nextElementSibling.id==='stArea'` 判定后 MutationObserver 搬移到进度条 |

### 主题 G · 筛选栏统一排版
| 版本 | 问题 | 修复 |
|---|---|---|
| v227.34 | 搜索框挤压日期框 | 搜索框独占一行、日期并排固定宽 |
| v227.61 / 62 | 移动端/PC 筛选控件风格分裂 | `filter-bar-m` 纵向分轨；PC 高特异性强制统一 |
| v227.74 | 仅 `.filter-bar-m` 11 模块统一，入库/订单漏网 | 扩展 `.filter-bar > .filter-row` 通用规则 + `orders.js` 重构 |
| v227.82 / 83 | 弹窗内/下拉框字号与筛选栏不一致 | 全部对齐 12px + 凹陷感模板 |

### 主题 H · SW / 离线 / 服务可用性（发布翻车重灾区）
| 版本 | 问题 | 修复 |
|---|---|---|
| v227.72 | SW 把 JS/CSS 回退成 HTML → 白屏 | 导航/资源分治 + 预缓存 + 写缓存 |
| v227.90 | 发版漏 bump `CACHE_NAME` → 旧 SW 缓存旧文件 | 白屏，用户硬刷新才解决 |
| v227.91 | `index.html` 引用戳未对齐已改文件 → 浏览器跑旧缓存 | `ReferenceError: progressBar is not defined` |
| v227.92 | 漏 bump `app-manifest.json` + `config.js` 版本 → 更新提示条永不弹 | bump 两文件 + SW 再 bump + 全量引用戳 |

### 主题 I · UIUX 微体验
| 版本 | 问题 | 修复 |
|---|---|---|
| v227.71 | 同步只有绿/灰点，过程中与失败长得一样 | 三态脉冲（频率承载语义：急/缓/待） |
| v227.71 | 空态纯灰字"暂无" | 给预期/给下一步的内联插画 + 引导 |
| v227.71 | 连续录入无"已记下"确认感 | 行底色闪绿 + 输入框 scale 脉冲（600ms 防残影）；`prefers-reduced-motion` 降级 |
| v227.71 | 点"刷新"白一下像点坏了 | 更新条滑入 + 按钮禁用+文案反馈 + 420ms 后 reload |

### 主题 J · v227.89–96（八轮修复 + 发布纪律）
| 项 | 问题 | 修复 |
|---|---|---|
| ① 删除未统一形状按钮 | 盘点页「清空已填」「放弃本次盘点」与其他按钮形状不一致 | 删除两按钮，新增 `#stProgressBar`（已盘 N/总数） |
| ② 移动端折叠按钮搬家 | 折叠列按钮在移动端位置不对 | MutationObserver 搬至进度条（见主题 F） |
| ③ 盘点号生成逻辑 | 删除记录后序号不回填 | 见主题 A |
| ④ 存货档案二维码按钮位置 | qr-actions 窄屏掉到二维码下方 | 提到 `qr-body` 同层；`@media(min-width:770px)` 强制不换行 |
| 发布 | 版本戳漏 bump（白屏/旧缓存/不提示）反复 3 次 | 版本戳六处同步 bump + grep 自检（见第五节） |
| 发布 | 回归门禁拉扯、绕过门禁假绿 | 过时测试移出/重写，发布前必真过 7/7 |
| v227.93 | ①订单列表 PC「全部状态」select 掉到第二行 ②日期弹窗点 ± 切月就自动确认（没选日期）③盘点往期修改路径仍弹「未结束盘点」④盘点记录操作列两按钮溢出 | ①3 个 select 合并回第一行并排 ②± 仅切视图、不写值/不关弹窗，只有点具体日才算确定 ③render 加「修改路径跳过提示」标志 ④操作列纵向 flex + >1366 视口横排 |
| v227.94 | 云端恢复报 `ConstraintError: Key already exists`（tempOutbound 主键冲突） | `clearAll()` 事务清单漏 v227.77 新增的 `tempOutbound` 独立 store → 补入事务与清空清单（典型「统一改造漏网」） |
| v227.95 | 「云端恢复（最新）」与「恢复上一份」两入口分散 | 合并为「☁️ 云端恢复」选择器：拉 bundle 列出最新 tables + 上一份 prevWork（双版本滚动本就存在），各显上传时间，点击恢复对应份 |
| v227.96 | 导入弹窗按钮文案旧（重新导入基准/上传并导入）+ 两列堆叠 | 「重新导入基准」→「导入基准」、「上传并导入」→「导入数据」（离线保留「仅导入本地」）；三按钮并排单行 |

---

## 三、反复出现的通病（跨主题归纳，按频次）

### 通病 1 · 版本戳漏 bump —— ★★★ 最致命，至少 4 次
`CACHE_NAME` / SW 注册戳 / `index.html` 全部 `?v=` / `config.js version` / `app-manifest.json version` 六处任意漏一处：
- v227.90 漏 `CACHE_NAME` → 白屏
- v227.91 引用戳没对齐已改文件 → 跑旧缓存 `ReferenceError`
- v227.92 漏 `app-manifest.json`+`config.js` → 更新提示永不弹
- （早期每版发布都靠人工对齐，极易漏）

### 通病 2 · 改动后不立刻实测，靠回归/E2E 才暴露 —— ★★
- `_relocateColCollapseToProgressBar` 误改模板漏声明变量 → 没 `node --check` 没起服务 → 回归才报 `ReferenceError`
- 折叠按钮 DOM 位置改了 **3 次**才定位（wrap 无 `.table-wrapper`、按钮挂 `#contentArea`）

### 通病 3 · DOM / 布局靠直觉，不实测 —— ★★
- 折叠按钮 `closest('[id]')`=`#stArea`、按钮挂在 `#contentArea`（与 `#stArea` 兄弟）→ 判定逻辑多次误判
- qr-actions `.qr-body` `flex-wrap` 窄屏换行掉到二维码下方
- iOS 防缩放 16px 与统一 12.5px 长期打架（v227.48 引入、v227.73/82 反复覆盖）

### 通病 4 · CSS `@media` 闭合括号误嵌套（静默 bug）—— ★★
- v227.69 `.as-input` 被漏闭合 `}` 吞噬 4 版本未生效（PC 文本框无凹陷感）
- v227.77 把闭合 `}` 误放 3585 行 → 3419~3584 桌面规则被关进移动端块 → v227.85 才修
- **根因**：`node --check` 语法合法、人工 grep 肉眼难辨 → 必须用浏览器解析 `cssRules` 实测（`scripts/audit-css.mjs`）

### 通病 5 · 统一改造覆盖不全，留漏网模块 —— ★★
- v227.74 只覆盖 `.filter-bar-m` 11 模块 → v227.75 补入库/订单
- 按钮统一审计出三套并行类，落地后仍有残留（`.btn--danger` 未全量替换内联红）
- v227.94 `clearAll()` 审计（AUDIT-002，v227.69）早于 `tempOutbound`（v227.77）出生 → 漏把它纳入清空清单 → 云端恢复主键冲突。新增 store/表后必须同步检查所有"全表遍历"清单（clearAll / TABLES / 回归种子）

### 通病 6 · 注释与实现不符，长期隐性债 —— ★
- v227.24 注释已写"删除记录序号继承前号"，但 `_genBatchNo` 一直 `max+1`，到 v227.91 才真修
- 多处"数据键约束"写一套、代码做一套（v217 方案审查 14 处源码矛盾）

### 通病 7 · 发布反复回炉 —— 对话层面
- 发布 → 打断做 GitHub → 刷新回归 → bump SW 重发 → 版本提示修复再发
- 根因：版本戳 + 回归 + 实测没在第一次做齐

### 通病 8 · 状态流转残留（盘点业务专属）—— ★★
- 开下一轮任务复活、概览残留、渲染读旧值、资源（序号/名额）不回收
- 见主题 A：每次改状态机必想"旧状态如何回收"

---

## 四、修复遵守规则（铁律，按优先级）

1. **发版前版本戳六处必全 bump**（通病 1 根因，最高优先级）——见第五节自检命令。`CACHE_NAME` / SW 注册 `?v=` / `index.html` 全部 `?v=` / `config.js version` / `app-manifest.json version` / 文件内容本身。
2. **写→验小步快跑**：每改一个文件 `node --check`；改完一个功能**立即起本地服务实测**，不堆到回归（通病 2）。
3. **验证前对齐引用戳 / 清缓存**，不在旧文件上调试（通病 1/2 衍生品）。
4. **DOM / 布局改动实测 DOM 树 + 几何**，PC/移动双态；CSS `@media` 改动跑 `scripts/audit-css.mjs` 解析 `cssRules`（通病 3/4）。
5. **统一改造必做"覆盖扫描"**：改一处样式/类，grep 全仓同类元素，确认无漏网模块（通病 5）。
6. **注释与实现矛盾当时修**，不留债（通病 6）。
7. **门禁真过 7/7**，过时测试移出/重写，不绕过、不假绿（通病 7）。
8. **局部修改不污染其他模块**；按钮统一走 `.btn` 系统；删除/作废类改动想"资源释放"（序号/ID/名额是否回收）（通病 8）。
9. **改状态流转想回收**：盘点 open→closed→补盘→下一轮，每次都清"上一版状态的残留"（通病 8）。

---

## 五、发布门禁 Checklist（发版前必过，杜绝通病 1/2/7）

**版本戳六处自检**（任一输出不为同一号即未对齐）：
```bash
grep -oE "\?v=227\.[0-9]+" index.html | sort -u          # 应只剩一个号
grep -n "version:" js/config.js                            # 应与上同号
node -e "console.log(require('./releases/app-manifest.json').version)"  # 同上
grep -n "CACHE_NAME = " service-worker.js                  # 同上
# index.html 内 register('service-worker.js?v=xxx') 同上
```

- [ ] 六处版本戳已同步 bump 到同一号
- [ ] `node --check` 改动文件全过（无语法错误）
- [ ] `node scripts/audit-css.mjs` 通过（`misScoped:[]`，防 `@media` 误嵌套）
- [ ] 回归套件 **7/7 全绿**（非环境性失败已修，未绕过门禁）
- [ ] 关键改动已 Playwright 截图 + 几何断言（PC + 移动）
- [ ] 发布返回 `verified: true`
- [ ] 告知用户：未自动刷新可手动硬刷新（Ctrl+Shift+R）一次

> 过时测试（如基于旧行为模型的 `test-v22716-entry.js`）移出/重写，**不假绿**、不删源码。

---

## 六、一句话通性（给未来的自己）

> **反复栽的坑只有两个：发版漏 bump 版本戳、改动后没立刻实测。**
> 盘点业务的特殊坑是"状态流转残留"——每次改 open/closed/补盘/下一轮，都要把上一版状态清干净。
> 把"版本戳六处 + 语法校验 + CSS 误嵌套 gate + 本地实测 + 回归门禁"在第一次发布前做齐，就不会再出现白屏、旧缓存、不提示、反复回炉。

---

## 七、云端备份模型 & 源码打包纪律（给未来的自己）

### 7.1 云端数据包隔离（务必牢记）
云端工作数据分 **两个独立对象**（同一 Supabase bucket `workbench-data`，不同文件名），互不干扰：
- **`data.json`（工作包）**：`导入数据`（`pushAllToCloud`→`pushData`）写入；`云端恢复`/`恢复上一份`（`pullData`/`pullDataPrivate`）读取。双版本滚动：`pushAllToCloud` 推送前把云端现有 `tables` 降级为 `prevWork`（上一份），新数据成 `tables`（最新）——天然保留"最新 + 上一份"两份。
- **`base.json`（基准包）**：`标记基准`（`pushBase`）写入；`导入基准`（`pullBase`）读取。与 `data.json` 完全独立，任何函数不写对方文件。
- 另有两个独立包：`settings.json`（用户偏好 + 出库/临时出库列表）、`stocktake.json`（盘点进度），均不污染 `data.json` 业务表。
- ⚠️ 本地侧：`导入基准`/`云端恢复` 是破坏性覆盖本地工作数据（有确认弹窗）；但它们只动各自的云端包，云端两份始终独立。

### 7.2 源码打包铁律（v227.96 起）
> **每次发布/交付，源码包必须永远包含：① 全部源码（`js/`、`css/`、`index.html`、`service-worker.js`、`releases/app-manifest.json`）② `全局代码审查标准.md` ③ `修复与发布纪律`文档。**
> 固化脚本：`scripts/package-source.mjs` —— 动态递归收集全部代码（新增文件自动纳入），并对两份文档做**存在性硬校验**（缺失即失败，杜绝漏打）。打包产物：`releases/warehouse-source-<version>.zip`。
>
> **体积红线（v227.96 起）**：源码包只为「上传 GitHub / 源码审查」服务，**只含【全部应用源码(js/ css/ lib/) + 应用壳与部署配置 + 两份必备文档】**。根级仅白名单（应用壳 + 部署配置 + 《修复与发布纪律》），其余根级文件（流程图 html / 审查报告(.md) / 截图 / 杂项）一律剔除；明确排除：无用图标(设计稿等)、解密桥接(bridge)、构建维护脚本(scripts)、截图与一切二进制——否则包太大传不上去。图标只保留 manifest 引用的 `icon-<尺寸>.png` 与同名矢量版 `icon-<尺寸>.svg`（共 14 个 PWA 图标），`icon-design.svg` 等设计稿剔除。新增应用代码落在 js/css/lib 下会自动纳入；若未来新增顶层代码目录，需在 `package-source.mjs` 的排除集里登记。
> 🔵 v227.97 修正：打包脚本原把 `assets/` 整体排除，但 `assets/login-bg/*.svg`（登录页背景，被 `js/app.js`、`css/style.css` 引用）是**运行必需资源**——缺它会导致 GitHub 部署后登录背景 404。已从 `EXCLUDE_DIRS` 移除 `assets` 并豁免其二进制剔除；`icon-design.svg` 等设计稿仍按 `USEFUL_ICON` 正则剔除。凡是代码里 `src/href` 或 JS 字符串引用的本地资源，都必须在包内（判包齐全的唯一标准：代码引用资源 ⊆ 包内文件）。

### 7.3 「工作台源码下载」产出旧代码的根因与防护（v227.96 修复）

> **现象**：从工作台点「源码下载」拿到的 zip 仍是旧版（如 4 按钮 `data-loader.js`，恢复上一份/云端恢复按钮未合并），但 `/workspace` 与线上部署明明已是合并版。
>
> **两条根因**：
> 1. **CDN / Service Worker 缓存未破坏**：`doDynamicDownload()` 里 `fetch(path)` 不带缓存破坏，浏览器/Service Worker 可能直接返回旧的 `data-loader.js`。→ 已在 `index.html` 给每个文件 fetch 加 `?_=Date.now()` 时间戳（v227.96）。
> 2. **静态托管无目录列表 + 缺权威清单**：下载逻辑优先「目录扫描」，但 GitHub Pages 等静态 CDN 不支持目录列表 → 回退到 `SOURCE_FILES.json`；该清单此前被 `.gitignore` 删除 → 404 → 再回退到硬编码 `FILES_TO_PACKAGE` 子集（可能漏文件）。
>
> **防护铁律**：
> - `package-source.mjs` 每次打包**重写**根目录 `SOURCE_FILES.json`（与包内文件集完全一致的全量清单），并随部署/提交带上（已从 `.gitignore` 解除忽略）。
> - **「源码下载」清单优先级已改为以 `SOURCE_FILES.json` 为准**（v227.96 修复）：原逻辑优先「目录扫描」，但工作台部署的静态托管恰好支持目录列表 → 走了扫描路径，其 `_isDevJunk` 规则把**根级 `.md`（即《修复与发布纪律》文档）误剔**，导致下载缺文档。现改为 SOURCE_FILES.json 权威优先，所有回退路径强制纳入两份必备文档。
> - 静态托管上的「源码下载」= `SOURCE_FILES.json`（现在永远是最新全量，含两份文档 + 有用图标）→ 下载到的就是脚本打的同一份包。
> - 交付 GitHub 用 `releases/warehouse-workbench-main-<version>.zip`（带 `warehouse-workbench-main/` 前缀，直接落到仓库根）；直接取源码用 `releases/warehouse-source-<version>.zip`（平铺）。
> - **永远用脚本产物交付，不要手拼 zip**——手拼会出现「戳新文件旧」（版本号新、文件内容旧）的灾难。

### 7.4 出库单跨设备不同步（v227.96 修复）

> **现象**：GitHub 部署已连接云端，但「出库单列表」仍空白；点「☁️ 云端恢复」反而有数据。
>
> **根因（云端双存储通道分流）**：出库单在云端有两条写入/读取通道，且启动恢复只读其中一条——
> - 启动自动恢复 `restoreOutboundFromSettings()`（`db.js`）只读 `settings.json` 的 `outbound_list` 键；
> - 全量「数据导入」走 `pushAllToCloud()`，把出库单写进 **`data.json` 的 `tables.outbound`**；
> - `syncOutboundToSettings()`（写 `outbound_list`）**仅在「导入出库 Excel」路径**被调用；实时单条同步 `pushOutboundToCloud()` 是**死代码（无调用点）**。
>
> 于是：经全量导入同步到云端的出库单躺在 `data.json`，而 GitHub 部署（全新、依赖启动恢复）只读 `settings.json`（为空）→ 列表空白。
>
> **修复**：`restoreOutboundFromSettings()` 在 `settings.json` 为空时，**兜底拉取 `data.json` 的 `tables.outbound`**（`SyncManager.pullDataPrivate()`）。两条通道的出库单在启动恢复时都能被拉回。
>
> **⚠️ 易混点**：「新增出库单」表单（`saveOrder`）写入的是**临时出库单**（`db.tempOutbound`，独立于「中心出库列表」`db.outbound`），二者互不影响。若你在「中心出库列表」看不到刚录入的单，先确认录入的是临时出库还是中心出库。

> **演进（同轮 v227.96）**：用户进一步指出——「每次进入工作台，已连云端时就应从云端自动同步最新的工作/设置/盘点三包，而非只靠手动「云端恢复」」。原 `restoreFromCloudWork` 自带「清空本地」危险确认弹窗，不能在启动时自动调用。故新增 `DataLoader.autoSyncFromCloud()`：**静默、时间戳门控**地拉取 `data.json` 工作包并应用——仅当云端 `savedAt` 比本机上次同步基线（`_cloudBase`）更新才覆盖本地，避免误清本机未上传改动；GitHub 等全新设备（无基线）首次进入即自动拉满。启动流程（`app.js`）在 `isOnline` 时依次调用 `autoSyncFromCloud()`（工作包）+ `restoreOutboundFromSettings`/临时出库/搜索历史（设置包）+ `StocktakeModule.pullCloudRecords()`（盘点包），实现进入即三包自动同步。

### 7.5 数据包边界铁律（v227.97 起，严禁混淆/污染）

> **用户铁律**：**每个数据包各管各的，恢复路径一一对应上传路径；出库单只走设置包，绝不进工作包；不允许一份数据落在两个包。**
>
> **边界定义（四包）**：
> | 包 | 通道/字段 | 内容 | 上传 | 恢复 |
> |---|---|---|---|---|
> | 工作包 `data.json` | `DataLoader.WORK_TABLES`（9 张：suppliers/orders/inbound/stock/inventoryAlerts/orderChecks/pricing/lowTurnover/breach） | 主数据导入/日常累积 | `pushAllToCloud()` | `loadBundleFromCloud()` / `autoSyncFromCloud()` |
> | 基准包 `base.json` | 同 `WORK_TABLES` | 系统底账 | `pushBase()`（标记基准） | `seedFromBase()` / 重新导入基准 |
> | 设置包 `settings.json` | `outbound_list` / `temp_outbound_list` / 搜索历史 / 主题 / 盘点任务 | 出库单等用户长期偏好 | `syncOutboundToSettings()` / `syncTemporaryOutboundToSettings()` | `restoreOutboundFromSettings()` 等 |
> | 盘点包 `stocktake.json` | `stocktake_records` | 盘点进度 | `StocktakeModule` | `pullCloudRecords()` |
>
> **v227.97 修复（回归事故）**：上轮（v227.96）为救「出库单不同步」，让 `restoreOutboundFromSettings()` 兜底读 `data.json.tables.outbound`，且 `pushAllToCloud()` 仍把 `outbound` 纳入工作包 `TABLES`（11 张）。结果——`data.json` 的工作包还原（`loadBundleFromCloud`/`autoSyncFromCloud`）会**用工作包里仅 1 条的 outbound 整体清空+覆盖本地**，而云端 256 条出库单其实只在 `settings.json`（`_updatedAt` 未变，数据未丢）。即「恢复路径用错包 + 一份数据落两包」共同造成的污染。
>
> **v227.97 根治措施（缺一不可）**：
> 1. `DataLoader` 新增 `SETTING_TABLES=['outbound','tempOutbound']` 与 getter `WORK_TABLES`（= `TABLES` 排除设置表）。
> 2. `loadBundleFromCloud()`（工作包还原）：只清/写 `WORK_TABLES`，事务内**不再调用 `clearAll()` 清全部**，保护 outbound/tempOutbound。
> 3. `pushAllToCloud()` / `pushBase()` / `seedFromBase()` / `_validateBundle()`：只遍历 `WORK_TABLES`，出库单**永不进 `data.json`/`base.json`**。
> 4. `restoreOutboundFromSettings()`：**只从 `settings.json` 恢复，删除一切读 `data.json` 的兜底**（恢复路径=上传路径）。
> 5. `syncOutboundToSettings()`：加**同包并集保护**——先拉云端 `outbound_list`，按业务键「出库单号|存货编码」合并后回写，避免本地被工作包还原清成少量后一同步就把云端 256 条覆盖掉（仅 settings 单通道内合并，不跨包）。
> 6. 新增 `DataStore.clearWorkTables()`（只清 `WORK_TABLES`），5 处工作包相关清空（base 垫底/全量导入/导入基准/forceSync/还原）改用它，不再误清设置包表。
> 7. 云端 `data.json`/`base.json` 历史残留的 `tables.outbound`/`tables.tempOutbound` 字段已清理删除（保留原 savedAt），使工作包彻底只含工作表。
>
> **⚠️ 红线**：新增任何「出库单/临时出库」的写入或恢复逻辑，只能落在 `settings.json` 通道；任何「工作包」的还原/推送/清空只许碰 `WORK_TABLES`。再出现「出库单进 data.json」或「用工作包字段恢复出库单」= 直接违反本铁律，必须打回。
